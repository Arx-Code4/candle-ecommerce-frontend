import type { FC } from 'react';
import type { CartItem } from '@/types';

interface CartItemRowProps {
  item: CartItem;
}

const CartItemRow: FC<CartItemRowProps> = () => null;

export default CartItemRow;
