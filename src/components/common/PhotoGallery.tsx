import type { FC } from 'react';
import type { ProductPhoto } from '@/types';

interface PhotoGalleryProps {
  photos: ProductPhoto[];
}

const PhotoGallery: FC<PhotoGalleryProps> = () => null;

export default PhotoGallery;
