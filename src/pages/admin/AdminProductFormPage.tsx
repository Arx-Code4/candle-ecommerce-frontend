// DEFAULT (decision #5, not yet confirmed): one shared page for both
// create and edit, branching on whether an :id route param is present,
// rather than two separate page components.
export default function AdminProductFormPage() {
  return null;
}
